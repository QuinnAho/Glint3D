Place Emscripten build outputs here:

- objviewer.js
- objviewer.wasm
- objviewer.data (if present)

These are generated by the web build of the engine. The UI references `/engine/objviewer.js` from `index.html`.

