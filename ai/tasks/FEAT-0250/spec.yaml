id: FEAT-0250
title: TextureView + MSAA Resolve Support
owner: ai
status: planned
created: "2025-09-13T00:00:00Z"
priority: high

description: >
  Add TextureView abstraction and multisample resolve path consistent with WebGPU.
  Ensure GL backend supports TextureView and explicit resolve in RenderPassEncoder.

acceptance_criteria:
  - "TextureView supported in descriptors and BindGroups"
  - "MSAA resolve via RenderPassEncoder resolveAttachment semantics"
  - "GL backend implements resolve with validation"
  - "Unit tests cover resolve and view formats"

must_requirements:
  - TEST.CROSS_PLATFORM
  - API.PUBLIC_HEADERS_PATH

plan:
  - id: PR1
    title: "Introduce TextureView and resolve descriptors"
    status: planned
    files: ["engine/include/glint3d/rhi*.h", "engine/src/rhi/**"]
  - id: PR2
    title: "GL backend TextureView + resolve"
    status: planned
    files: ["engine/src/rhi/rhi-gl.cpp", "tests/unit/rhi_test.cpp"]

file_whitelist:
  - "engine/src/rhi/**"
  - "engine/include/glint3d/rhi*.h"
  - "tests/unit/rhi_test.cpp"
  - "docs/rhi_architecture.md"

outputs:
  - engine/include/glint3d/rhi.h
  - docs/rhi_architecture.md

dependencies:
  blocked_by: [FEAT-0248]

