#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

// Minimal stub for single-file static use.
// Recent miniz headers expect this when building as a library,
// but for our static use we can leave these empty.
#ifndef MINIZ_EXPORT
#define MINIZ_EXPORT
#endif

#ifndef MINIZ_NO_EXPORT
#define MINIZ_NO_EXPORT
#endif

#endif // MINIZ_EXPORT_H

